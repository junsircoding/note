根据类的名称字符串动态调用类

```python
# eval函数
class MyCls:
    def __init__(self, p1):
        self.p1 = p1
    def show(self):
        print(self.p1)


cls_name = "MyCls"
NewCls = eval(cls_name)
new_cls = NewCls(p1="test_p1")
new_cls.show()
```